function [out_features] = integration_features(window_data)
%INTEGRATION_FEATURES Integrating accelerometer data to obtain rough
% velocity and position 'increments'
%   Exact values and dimesions of velocity/position not important
% output features in the format of a design matrix
% 
    vel = squeeze(trapz(x))';



end

