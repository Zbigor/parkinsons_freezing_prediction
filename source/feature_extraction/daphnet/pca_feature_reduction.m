function [out_design_matrix] = pca_feature_reduction(design_matrix)
%PCA_FEATURE_REDUCTION Dimensionality reduction using PCA
%   Full design matrix (rows = observation, columns = features) is
% expected on input, with data from all windows from the training set





end

